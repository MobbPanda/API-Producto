# CONEXIÓN LOCAL A POSTGRES
# spring.datasource.url=jdbc:postgresql://localhost:5432/petsocity_productos
# spring.datasource.username=postgres
# spring.datasource.password=admin

# ============================
# CONFIGURACIÓN DE BASE DE DATOS (RAILWAY)
# ============================

spring.datasource.url=jdbc:postgresql://${PGHOST:${DATABASE_HOST}}:${PGPORT:${DATABASE_PORT}}/${PGDATABASE:${DATABASE_NAME}}
spring.datasource.username=${PGUSER:${DATABASE_USER}}
spring.datasource.password=${PGPASSWORD:${DATABASE_PASSWORD}}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# PUERTO DEL SERVIDOR
server.port=${PORT:8080}